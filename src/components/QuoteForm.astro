---
import Icon from './Icon.astro'
import Eyebrow from './Eyebrow.astro'
import { whatsapp } from '../data/site'

const trust = [
  { icon: 'clock', label: 'Resposta rápida' },
  { icon: 'shield', label: 'Apoio técnico especializado' },
  { icon: 'map-pin', label: 'Atendimento em todo o Brasil' },
] as const

const whatsappHref = whatsapp
  ? `https://wa.me/${whatsapp}?text=${encodeURIComponent('Olá! Gostaria de solicitar uma cotação de rolamentos.')}`
  : ''
---

<section id="cotacao" class="bg-neutral-gray-100 pt-8 pb-20">
  <div class="mx-auto max-w-page px-6 lg:px-10">
    <div class="grid gap-10 lg:grid-cols-[minmax(0,1.05fr)_minmax(0,1fr)] lg:items-center lg:gap-16">
      {/* Espaço reservado ao shortcode do formulário */}
      <div class="order-2 w-full lg:order-1">
        <slot name="shortcode">
          <div class="flex min-h-[520px] w-full flex-col items-center justify-center gap-3 rounded-2xl border border-dashed border-primary-dark/25 bg-neutral-light p-8 text-center shadow-[0_14px_32px_-26px_rgba(0,24,48,0.5)]">
            <span class="font-sans text-[11px] font-semibold tracking-[0.18em] text-accent-orange uppercase">
              Espaço reservado
            </span>
            <p class="font-display text-lg font-bold text-ink">Shortcode — formulário de cotação</p>
            <p class="max-w-xs font-sans text-sm leading-relaxed text-ink-soft">
              Insira o conteúdo no slot <code class="font-mono text-ink">shortcode</code>
              do componente QuoteForm.
            </p>
          </div>
        </slot>
      </div>

      {/* Texto de contato */}
      <div class="order-1 lg:order-2">
        <Eyebrow>Solicite sua cotação</Eyebrow>

        <h2 class="mt-3 font-display text-3xl leading-tight font-extrabold text-ink sm:text-4xl">
          Fale com quem entende de rolamentos industriais.
        </h2>

        <span aria-hidden="true" class="mt-5 block h-[3px] w-16 bg-accent-orange"></span>

        <p class="mt-5 font-sans text-[15px] leading-relaxed text-ink-soft sm:text-base">
          Nossa equipe está pronta para identificar, especificar e cotar o rolamento certo para a
          sua aplicação. Preencha o formulário ou fale direto conosco pelo WhatsApp.
        </p>

        <div class="mt-6 flex flex-col gap-3 sm:flex-row sm:items-center">
          {whatsappHref ? (
            <a
              href={whatsappHref}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center gap-2.5 bg-primary-dark px-6 py-3.5 font-sans text-[15px] font-semibold text-neutral-light transition-colors duration-200 hover:bg-primary-dark-800"
            >
              <Icon name="whatsapp" class="size-4 shrink-0" />
              Chamar no WhatsApp
            </a>
          ) : (
            <span class="inline-flex items-center gap-2.5 bg-primary-dark px-6 py-3.5 font-sans text-[15px] font-semibold text-neutral-light/70">
              <Icon name="whatsapp" class="size-4 shrink-0" />
              WhatsApp em breve
            </span>
          )}
        </div>

        <ul class="mt-7 flex flex-wrap gap-x-6 gap-y-2.5">
          {trust.map((item) => (
            <li class="flex items-center gap-2 font-sans text-[13px] font-medium text-ink">
              <Icon name={item.icon} class="size-4 shrink-0 text-accent-orange" />
              {item.label}
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>
</section>
