---
// Carrossel infinito de logos de marcas — 5 itens visíveis por vez
// (largura de cada item = 1/5 da largura visível, via container query).
import { Image } from 'astro:assets'
import timken from '../assets/brands/timken.webp'
import iko from '../assets/brands/iko.webp'
import ntn from '../assets/brands/ntn.webp'
import brm from '../assets/brands/brm.webp'
import rolmax from '../assets/brands/rolmax.webp'
import nsk from '../assets/brands/nsk.webp'
import skf from '../assets/brands/skf.webp'

const brands = [
  { name: 'Timken', src: timken },
  { name: 'IKO Thompson Brasil', src: iko },
  { name: 'NTN', src: ntn },
  { name: 'BRM', src: brm },
  { name: 'Rolmax', src: rolmax },
  { name: 'NSK', src: nsk },
  { name: 'SKF', src: skf },
] as const
---

<div class="brand-carousel relative overflow-hidden">
  <div class="brand-carousel-track flex items-center">
    {/* Lista duplicada duas vezes para o loop infinito ficar contínuo */}
    {[0, 1].map((copy) => (
      <ul class="flex shrink-0 items-center" aria-hidden={copy === 1 ? 'true' : undefined}>
        {brands.map((brand) => (
          <li class="brand-item flex h-14 shrink-0 items-center justify-center px-4">
            <Image
              src={brand.src}
              alt={brand.name}
              format="webp"
              loading="lazy"
              decoding="async"
              class="h-14 w-auto object-contain"
            />
          </li>
        ))}
      </ul>
    ))}
  </div>
</div>

<style>
  .brand-carousel {
    container-type: inline-size;
    -webkit-mask-image: linear-gradient(
      to right,
      transparent 0,
      black 48px,
      black calc(100% - 48px),
      transparent 100%
    );
    mask-image: linear-gradient(
      to right,
      transparent 0,
      black 48px,
      black calc(100% - 48px),
      transparent 100%
    );
  }

  .brand-carousel-track {
    width: max-content;
    animation: brand-scroll 26s linear infinite;
  }

  /* Cada item ocupa exatamente 1/5 da largura visível do carrossel,
     então sempre há 5 logos visíveis por vez, não importa a tela. */
  .brand-item {
    width: calc(100cqw / 5);
  }

  .brand-carousel:hover .brand-carousel-track {
    animation-play-state: paused;
  }

  @keyframes brand-scroll {
    from {
      transform: translateX(0);
    }
    to {
      /* Anda exatamente a largura de uma cópia da lista (50% do trilho) */
      transform: translateX(-50%);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .brand-carousel-track {
      animation: none;
    }
  }
</style>
